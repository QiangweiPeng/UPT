1. 将ot的结果保存成稀疏矩阵，节约内存，加快取batch时候的采样。也许可以在归一化前就设置个阈值把特别小的直接mask掉。
2. 现在训练的时候v的loss不降低，另外很容易出现nan。推测可能的原因是condition也需要通过神经网络来学习导致的。一个检查的方法时不学condition直接用，以及单独取出某个扰动来看看能不能学好。
3.当前用于计算配对的是X_pca，根根据这个挑选的delta，实际训练的时候选用的是缩放后的X_pca。